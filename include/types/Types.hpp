#pragma once

#include <cstddef>

using LineNumber = std::size_t;
using LayerSize = std::size_t;
using LayerCount = std::size_t;
using LayerIndex = std::size_t;
using InputSize = std::size_t;
using NeuronCount = std::size_t;
using EpochCount = std::size_t;
using BatchSize = std::size_t;
using ConfigCount = std::size_t;
using Scalar = double;
using LearningRate = Scalar;
using LossValue = Scalar;
using AccuracyScore = Scalar;
using EpochIndex = std::size_t;
